@model IEnumerable<ItlaFlixApp.WEB.Models.RolModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.cod_rol)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.txt_desc)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.sn_activo)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.cod_rol)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.txt_desc)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.sn_activo)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.cod_rol}) |
                @Html.ActionLink("Details", "Details", new { id=item.cod_rol}) |
                @Html.ActionLink("Delete", "Delete", new {  id=item.cod_rol  })
            </td>
        </tr>
}
    </tbody>
</table>
